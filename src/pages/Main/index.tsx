import React from "react";
import {P_PROPS_TYPE} from "../constants";

export default function Main({isAuth, userId, userName, userPass}: P_PROPS_TYPE) {


    return (
        <div className="main-container">

        </div>
    )
}
